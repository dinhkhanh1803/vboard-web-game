rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Foundation default: no file access until avatar and asset policies exist.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
