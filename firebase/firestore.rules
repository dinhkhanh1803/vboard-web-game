rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Foundation default: no client access until paths have emulator tests.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
